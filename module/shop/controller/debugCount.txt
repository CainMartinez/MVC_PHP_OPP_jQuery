SELECT COUNT(*)total 
		FROM property p INNER JOIN property_operation po ON p.id_property = po.id_property INNER JOIN operation o ON po.id_operation = o.id_operation AND o.id_operation = 2SELECT COUNT(*)total 
		FROM property p INNER JOIN property_category pc ON p.id_property = pc.id_property INNER JOIN category cat ON pc.id_category = cat.id_category AND cat.id_category = 1SELECT COUNT(*)total 
		FROM property p INNER JOIN property_category pc ON p.id_property = pc.id_property INNER JOIN category cat ON pc.id_category = cat.id_category AND cat.id_category = 1SELECT COUNT(*)total 
		FROM property p INNER JOIN property_category pc ON p.id_property = pc.id_property INNER JOIN category cat ON pc.id_category = cat.id_category AND cat.id_category = 1 INNER JOIN city c ON p.id_city = c.id_city AND c.id_city = 1